<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="Expander.Static.Arrow.Stroke"
                     Color="{StaticResource TabColor}" />
    <SolidColorBrush x:Key="Expander.Static.Arrow.Fill"
                     Color="Transparent" />
    <SolidColorBrush x:Key="Expander.MouseOver.Arrow.Stroke"
                     Color="{StaticResource TabMouseOverColor}" />
    <SolidColorBrush x:Key="Expander.MouseOver.Arrow.Fill"
                     Color="{StaticResource TabMouseOverColor}" />
    <SolidColorBrush x:Key="Expander.Pressed.Arrow.Stroke"
                     Color="{StaticResource TabColor}" />
    <SolidColorBrush x:Key="Expander.Pressed.Arrow.Fill"
                     Color="{StaticResource TabColor}" />
    <SolidColorBrush x:Key="Expander.Disabled.Arrow.Stroke"
                     Color="{StaticResource TabColor}" />

    <SolidColorBrush x:Key="Border.MouseOver.Background"
                     Color="#4C5D5D5D" />


    <Style TargetType="Expander"
           x:Key="ListViewExpanderStyle">
        <Setter Property="SnapsToDevicePixels"
                Value="True" />
        <Setter Property="UseLayoutRounding"
                Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <StackPanel SnapsToDevicePixels="True"
                                UseLayoutRounding="True"
                                Margin="{TemplateBinding Margin}">
                        <ToggleButton x:Name="ExpanderToggle"
                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      Focusable="False"
                                      HorizontalContentAlignment="Left"
                                      Padding="4,2"
                                      Content="{TemplateBinding Header}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border>
                                        <StackPanel Orientation="Horizontal">
                                            <Border x:Name="ToggleBorder"
                                                    CornerRadius="1">
                                                <Path x:Name="Arrow"
                                                      Width="15"
                                                      Height="15"
                                                      Stroke="{StaticResource Expander.Pressed.Arrow.Stroke}"
                                                      Fill="{StaticResource Expander.Pressed.Arrow.Fill}"
                                                      Stretch="Uniform"
                                                      HorizontalAlignment="Left"
                                                      Data="M 0 0 L 10 0 L 5 5 Z"
                                                      RenderTransformOrigin="0.5,0.5">
                                                    <Path.RenderTransform>
                                                        <RotateTransform Angle="0" />
                                                    </Path.RenderTransform>
                                                </Path>
                                            </Border>
                                            <ContentPresenter VerticalAlignment="Center"
                                                              RecognizesAccessKey="True"
                                                              Margin="2 0 0 0" />
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked"
                                                 Value="False">
                                            <Setter TargetName="Arrow"
                                                    Property="RenderTransform">
                                                <Setter.Value>
                                                    <RotateTransform Angle="-90" />
                                                </Setter.Value>
                                            </Setter>
                                            <Setter TargetName="Arrow"
                                                    Property="Stroke"
                                                    Value="{StaticResource Expander.Static.Arrow.Stroke}" />
                                            <Setter TargetName="Arrow"
                                                    Property="Fill"
                                                    Value="{StaticResource Expander.Static.Arrow.Fill}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver"
                                                 Value="True">
                                            <Setter TargetName="Arrow"
                                                    Property="Stroke"
                                                    Value="{StaticResource Expander.MouseOver.Arrow.Stroke}" />
                                            <Setter TargetName="Arrow"
                                                    Property="Fill"
                                                    Value="{StaticResource Expander.MouseOver.Arrow.Fill}" />
                                            <Setter TargetName="Arrow"
                                                    Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#CC000000"
                                                                      BlurRadius="10"
                                                                      ShadowDepth="0"
                                                                      Opacity="1" />
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ExpanderContent"
                                          Margin="0"
                                          ContentSource="Content" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded"
                                 Value="False">
                            <Setter TargetName="ExpanderContent"
                                    Property="Visibility"
                                    Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>